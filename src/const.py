# the variables below contains all of the constant variables the bot will need during runtime.

keys_location = 'E:/keys'
bot_prefix = '$'

# bot features can be disabled or enabled here:

feature_bard = True
feature_spyfall = True
feature_wolframalpha = True
# bard also includes the latex command.